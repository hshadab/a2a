# Core dependencies
fastapi>=0.104.0
uvicorn>=0.24.0
httpx>=0.25.0
pydantic>=2.5.0

# Database
# psycopg3 preferred for better Render PostgreSQL SSL compatibility
psycopg[binary]>=3.1.0
psycopg-pool>=3.1.0
asyncpg>=0.29.0  # Fallback if psycopg unavailable
redis>=5.0.0

# Blockchain (Base mainnet x402)
web3>=6.11.0
eth-account>=0.10.0

# Utilities
python-dotenv>=1.0.0

# ML/zkML
onnxruntime>=1.16.0
numpy>=1.24.0

# A2A Protocol (Google Agent-to-Agent)
# Note: a2a-sdk is optional, we implement A2A v0.3 natively
# a2a-sdk>=0.3.22

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
respx>=0.20.0
